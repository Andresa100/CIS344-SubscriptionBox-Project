# SQL Scripts

This folder contains SQL scripts used to create and manage the Subscription Box Service database.

Files included:
- schema.sql → Creates the database and tables
- (Optional) data.sql → Inserts sample data
- (Optional) queries.sql → Sample SQL queries
